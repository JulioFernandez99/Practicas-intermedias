import { Routes } from '@angular/router';
import { UsuarioComponent } from './components/registro/usuario/usuario/usuario.component';

export const routes: Routes = [
    {
        path: 'registro/usuario',
        component: UsuarioComponent
    }
];
